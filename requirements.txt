# Use `pip install -r requirements.txt` in a Python env.
# For mamba: do not pass this file to `create --file` (conda cannot solve torch/PyG);
#           use `mamba env create -f environment.yml` instead.
#
# If pip tries to compile torch_scatter/torch_cluster, set PIP_FIND_LINKS to the
# matching wheel page (see https://data.pyg.org/whl/) for your torch+cuda build.
torch>=2.2.0
torch_geometric>=2.5.0
torch_cluster>=1.6.3
torch_scatter>=2.1.2
numpy>=1.26.0
scipy>=1.12.0
einops>=0.7.0
tqdm>=4.66.0
pyyaml>=6.0.1
laspy[laszip]
tensorboard>=2.20.0
setuptools<82  # setuptools 82+ drops pkg_resources; tensorboard CLI still needs it
